import 'package:flutter/material.dart';

abstract class ColorPalette {
  static const primary = Color(0xFF007CFF);
  static const textPrimary = Color(0xFF1E1E1E);
  static const textDisable = Color(0xFF828F9C);
}
